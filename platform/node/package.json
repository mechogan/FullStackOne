{
  "name": "@fullstacked/editor-node",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild build.ts --outfile=.cache/build.js --packages=external && node .cache/build.js",
    "start": "npm run build && node .cache/index.js"
  },
  "type": "module",
  "dependencies": {
    "esbuild": "^0.19.11",
    "mime": "^4.0.1"
  }
}
