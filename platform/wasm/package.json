{
    "scripts": {
        "build": "node build.js",
        "start": "npm run build && node dev.js",
        "deploy": "npm run build && npx wrangler pages deploy out"
    },
    "type": "module",
    "dependencies": {
        "@types/winbox": "^0.2.5",
        "mime": "^4.0.6",
        "winbox": "^0.2.82"
    }
}
