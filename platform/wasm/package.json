{
    "name": "@fullstacked/editor-wasm",
    "version": "0.9.0",
    "scripts": {
        "build": "node build.js",
        "start": "npm run build && http-server -o"
    },
    "type": "module",
    "devDependencies": {
        "http-server": "^14.1.1"
    }
}
